pipeline {
    agent any

    parameters {
        choice(
            choices: 'worker01\nworker02\nworker03',
            description: 'Select the worker',
            name: 'WORKER'
        )
    }

    stages {
        stage('Run rsync on Worker') {
            steps {
                script {
                    def worker = params.WORKER
                    echo "Selected worker: $worker"

                    // Define the target host and SSH credentials ID
                    def user = "root"
                    def targetHost = "192.168.1.210"
                    def sshCredentials = "jenkins-private-key-base64"

                    // Execute rsync command using withCredentials block
                    withCredentials([
                        sshUserPrivateKey(credentialsId: sshCredentials, keyFileVariable: 'SSH_PRIVATE_KEY')
                    ]) {
                        sh "echo '\${SSH_PRIVATE_KEY}' | base64 --decode > ssh_key"
                        sh "chmod 400 ssh_key"
                        sh "rsync -a -e 'ssh -o StrictHostKeyChecking=no -i ssh_key' /mnt/ssd/nfs/worker-base-template/ root@${targetHost}:/mnt/ssd/nfs/${worker}"
                    }
                }
            }
        }
    }
}
