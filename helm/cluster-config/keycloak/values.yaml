keycloak:
  postgresql:
    auth:
      password: "keycloak-test123"
  auth:
    adminUser: "admin"
    adminPassword: "keycloak-test123"
  ingress:
    enabled: true
    hostname: keycloak.myrecipes.at
    servicePort: https
  tls:
    enabled: true
    autoGenerated: true
