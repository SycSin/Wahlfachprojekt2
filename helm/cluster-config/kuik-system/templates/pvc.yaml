---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: data-kube-image-keeper-registry-0
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi