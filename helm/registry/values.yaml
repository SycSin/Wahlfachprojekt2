persistence:
  enabled: "true"
  size: "20Gi"
  accessMode: "ReadWriteOnce"
  storageClass: "nfs-csi"

ingress:
  host: "registry"
  domain: "myrecipes.at"
  path: "/"
  serviceName: "registry-docker-registry"
  servicePort: "5000"
